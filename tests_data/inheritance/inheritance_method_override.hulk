type Parent
{
    greet() => "Hello from Parent";
}

type Child inherits Parent
{
    greet() => "Hello from Child";
}

{
    let p = new Parent() in print(p.greet());
    let c = new Child() in print(c.greet());
}
