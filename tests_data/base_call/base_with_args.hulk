type Calculator
{
    compute(a, b) => a + b;
}

type AdvancedCalculator inherits Calculator
{
    compute(a, b) => base(a, b) * 2;
}

let ac = new AdvancedCalculator() in
    print(ac.compute(3, 4));
