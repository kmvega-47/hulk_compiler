function add(x: Number, y: Number): Number => x + y;
function double(x: Number): Number => x * 2;

{
    // call_param_type_mismatch_simple
    print(sqrt("hola"));

    // call_param_type_mismatch_multiple
    print(add(true, "texto"));

    // call_incorrect_use
    print(3 + double(5));
}