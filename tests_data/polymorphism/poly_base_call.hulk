type Animal
{
    sound() => "sound";
}

type Dog inherits Animal
{
    sound() => base() @ " woof";
}

type Cat inherits Animal
{
    sound() => base() @ " meow";
}

{
    let d = new Dog() in print(d.sound());
    let c = new Cat() in print(c.sound());
}
