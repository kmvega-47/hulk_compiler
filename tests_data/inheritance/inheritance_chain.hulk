type A
{
    name() => "A";
}

type B inherits A
{
    name() => "B";
}

type C inherits B
{
    name() => "C";
}

{
    let a = new A() in print(a.name());
    let b = new B() in print(b.name());
    let c = new C() in print(c.name());
}
