type Point 
{
    x = 0;
    getX() => self.x;

    test() 
    {
        let self = new Point() in 
            self.x;
    }
}

// acceso no privado: intentar acceder desde fuera del tipo
let p = new Point() in 
    print(p.x);