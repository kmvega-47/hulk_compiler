type Point3D(x, y, z)
{
    x = x;
    y = y;
    z = z;

    sum() => self.x + self.y + self.z;
    product() => self.x * self.y * self.z;
}

let p = new Point3D(1, 2, 3) in
{
    print(p.sum());
    print(p.product());
}
