type Counter
{
    value = 0;

    increment()
    {
        self.value := self.value + 1;
    }

    get() => self.value;
}

let c = new Counter() in
{
    c.increment();
    c.increment();
    c.increment();
    print(c.get());
}
