type Animal(name)
{
    name = name;
    speak() => "...";
}

type Dog(name) inherits Animal(name)
{
    speak() => "Woof!";
}

type Cat(name) inherits Animal(name)
{
    speak() => "Meow!";
}

{
    let d = new Dog("Rex") in print(d.speak());
    let c = new Cat("Whiskers") in print(c.speak());
}
