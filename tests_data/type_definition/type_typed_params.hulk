type Point(x: Number, y: Number)
{
    x: Number = x;
    y: Number = y;

    getX(): Number => self.x;
    getY(): Number => self.y;
}

let pt = new Point(3, 4) in
{
    print(pt.getX());
    print(pt.getY());
}
